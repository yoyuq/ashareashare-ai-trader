# ====== A股智能分析Agent — 核心依赖 (2026-09 与 pyproject.toml 单源同步) ======
# 推荐安装方式: pip install -e ".[dev,all]" (以 pyproject.toml 为准, 本文件仅作只读镜像)

# ---- 数据获取 ----
akshare>=1.18.0
baostock>=0.8.8
tushare>=1.4.0            # 可选主源 (需 TUSHARE_TOKEN, 见 [all] extras)

# ---- 数值计算 ----
numpy>=1.26.0
pandas>=2.0.0
scipy>=1.12.0

# ---- 技术指标 ----
pandas-ta>=0.3.14b0       # 纯Python指标引擎 (缺失时 indicators.py 自动降级 numpy 实现)

# ---- 向量数据库 ----
chromadb>=0.5.0

# ---- LLM/Agent 编排 ----
langgraph>=1.0.0
openai>=2.0.0             # DeepSeek 走 openai SDK (base_url=api.deepseek.com)

# ---- Web框架 ----
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
streamlit>=1.35.0

# ---- 工具 ----
pydantic>=2.0.0
pyyaml>=6.0.0
jsonschema>=4.20.0        # trading_rules.yaml schema 校验 (knowledge/manager)
httpx>=0.27.0
loguru>=0.7.0
python-dotenv>=1.0.0
plotly>=5.20.0
tqdm>=4.66.0

# ---- 可选/平台特定 (不装不影响主链路) ----
# xtquant>=1.0.0          # QMT 实盘接口 (broker/live.py, 仅 Windows+QMT 环境)
# mcp>=1.0.0              # api/mcp_server.py MCP 接入
# uiautomation>=2.0.0     # notify/wechat_friend.py 微信通知
